---
dependencies:
  - role: package/ruby
    ruby_gems:
      - robot_sweatshop
  - role: package/upstart
    upstart_name: robot_sweatshop
    upstart_description: robot_sweatshop
    upstart_exec_path: /usr/local/bin/sweatshop
    upstart_exec_flags:
      - start

# New logging method awaiting mci regex formatting
#   - role: package/fluentd
#     fluentd_sources:
#       - path: "{{ mci_worker_log_path }}"
#         format: ''
#         tag: mci.worker
